apiVersion: v1
kind: Service
metadata:
  name: {{ include "temp-chart.fullname" . }}
  labels:
    {{- include "temp-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "temp-chart.selectorLabels" . | nindent 4 }}
