apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gestion-garage.fullname" . }}-test-connection"
  labels:
    {{- include "gestion-garage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gestion-garage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
