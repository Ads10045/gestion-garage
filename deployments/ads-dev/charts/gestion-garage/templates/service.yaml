apiVersion: v1
kind: Service
metadata:
  name: {{ include "gestion-garage.fullname" . }}
  labels:
    {{- include "gestion-garage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gestion-garage.selectorLabels" . | nindent 4 }}
